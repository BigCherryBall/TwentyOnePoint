package TwentyOnePoint;

public class Res21Game
{
    /* @NotNull 响应消息，本模块的响应只有字符串类型。该变量必定不为null */
    public String text;
    /* @NotNull 指令发出者所属的群组（同Req21Ganme.belong) */
    public Object belong;
    /* @NotNull 指令发出者（同Req21Ganme.sender) */
    public Object sender; 
}
